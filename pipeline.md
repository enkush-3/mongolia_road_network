## 🚀 Архитектурын Дамжлага (End-to-End Pipeline)

Энэхүү системд өгөгдөл хэрхэн уншигдаж, боловсруулагдаж, хэрэглэгчийн дэлгэцэнд очих дамжлагыг бүтэцчилэн үзүүлбэл:

1. **Өгөгдөл Бэлтгэх (QGIS Preprocessing):** Хөгжүүлэлтийн өмнөх шат.
   Том хэмжээний замын Shapefile-ыг **QGIS** дээр нээж, хэрэгцээгүй багануудыг устган цэвэрлээд, вэбд уншихад хамгийн тохиромжтой **GeoJSON** формат руу хөрвүүлж хадгална.


2. **Spring Boot-д Граф Усгах (Data Ingestion & Indexing):** Бэкенд асах үед.
   Сервер асах мөчид GeoJSON файлыг Jackson ашиглан уншина. Замын огтолцох цэгүүдийг (Координатуудыг таслалаас хойш 6 орноор дугуйлж) уулзвар болгон багцалж, санах ойд **Adjacency List** хэлбэртэй өөрийн замын графыг (Map


3. **React талын Хэрэглэгчийн Сонголт (Event Capture):** Хэрэглэгч карт дээр дарах үед.
   React дээрх Canvas дээр хэрэглэгч хулганаар Эхлэл ($Start$) болон Хүрэх ($End$) цэгүүдийг сонгоно. Тэдгээр цэгүүдийн газар зүйн координатыг ($Lat, Lng$) WebSocket холболтоор эсвэл HTTP Post хүсэлтээр Spring Boot сервер рүү шиднэ.


4. **Мап Мэтчинг ба Алгоритмын Тооцоолол (Multi-Threaded Routing Engine):** Сервер талд (Микросекундээр).
1. **Map Matching:** Spring Boot ирсэн координатуудад хамгийн ойр байгаа бодит замын оройнуудыг (Nearest Nodes) өөрийн санах ойн R-Tree-гээс маш хурдан шүүж олно.



2. **Зэрэгцээ тооцоолол:** Spring Boot-ийн ExecutorService (Thread Pool) ашиглан 3 өөр Thread дээр **Dijkstra**, **A***, **Greedy** алгоритмуудыг зэрэг ажиллуулна. Алгоритм тус бүрийн туулсан замын урт, явсан замын цэгүүд, ажилласан хугацааг тооцож багцална.


5. **Redis Төлөв Хадгалалт ба Сувгаар Илгээх (Message Broker):** Мэдээлэл дамжуулах үед.
   Хэрэв системд машин симуляци зэрэг ажиллаж байгаа бол замын шинэ төлөвүүд болон алгоритмын үр дүнг **Redis Pub/Sub** суваг руу (routing:results) болон Redis-ийн кэш рүү (ST_Distance тооцоололд зориулж) RedisTemplate ашиглан бичнэ. Spring Boot дэх WebSocket Handler нь Redis-ээс ирсэн энэ өгөгдлийг React руу илгээнэ.


6. **React-ийн real-time Дүрслэл (Rendering):** Фронтенд талд.
   React нь WebSocket-оор ирсэн 3 алгоритмын замын мэдээллийг хүлээж аваад state-ээ шинэчилнэ. HTML5 Canvas эсвэл Pixi.js ашиглан 3 алгоритмын замыг 3 өөр өнгөөр дэлгэцэнд зурж, хажууд нь аль алгоритм хэдэн миллисекундэд бодож дууссаныг тоон үзүүлэлтээр харьцуулан харуулна.

---

## 🛠️ Ашиглах Технологийн Стек (Tech Stack Setup)

Энэхүү дамжлагыг бодитоор хэрэгжүүлэхэд Spring Boot болон React дээр дараах сангуудыг ашиглавал ажил маш их хөнгөрнө:

### 1. Spring Boot (Backend)

* **Spring Web/WebSocket:** Хэрэглэгчийн хайлтын хүсэлтийг авах болон real-time өгөгдлийг вэб рүү маш бага хоцролттой (low latency) дамжуулахад ашиглана.
* **Spring Data Redis:** Redis сервертэй холбогдож Pub/Sub болон түгжрэлийн датаг секунд тутамд кэшлэхэд ашиглана.
* **JTS Topology Suite (Java Topology Suite):** Энэ бол Java дээрх гео-өгөгдөлтэй ажилладаг хамгийн хүчирхэг сан юм. Үүнийг ашиглан замуудын огтолцол олох, хэрэглэгчийн дарсан цэгт хамгийн ойр замын оройг хайх (R-Tree index) зэргийг маш хялбар шийднэ.

### 2. React (Frontend)

* **React Context / Redux Toolkit:** Серверээс ирж буй секунд тутамд шинэчлэгдэх замын төлөв, машины координатуудыг удирдах өгөгдлийн төв.
* **HTML5 Canvas API эсвэл Pixi.js:** Асар том газар зүйн өгөгдлийг React-ийн энгийн DOM элементээр зурвал вэб гацах тул Canvas ашиглан график карт (GPU) дээр шууд зурна. Pixi.js нь Canvas-ийг маш хурдан ажиллуулдаг бэлэн React дэмжлэгтэй сан юм.

---
